{
  "rules": {
    ".read": true,
    "comments": {
      "$commentID": {
        ".write": "auth != null && !data.exists()",
        ".validate": "newData.hasChildren(['userid', 'body']) && newData.child('body').isString() && auth.id == newData.child('userid').val()"
      }
    }
  }
}
